{
  "scripts": {
    "build": "cd server && npm install && npm run build-client",
    "start": "cd server && npm start"
  },
  "postinstall": "cd server && npm install",
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "@google/genai": "^0.9.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "memcached": "^2.2.2",
    "mime": "^4.0.4",
    "pg": "^8.11.5"
  }
}